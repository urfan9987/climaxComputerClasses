<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .dashboard-card {
            background-color: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .profile-img {
            border-radius: 50%;
            width: 100px;
            height: 100px;
            object-fit: cover;
        }
    </style>
</head>
<body>

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="dashboard-card">
                <div class="row">
                    <div class="col-md-4 text-center">
                        <img src="data:image/png;base64,<%= user.dp %>" alt="Profile Picture" class="profile-img mb-3">

                        <h5><%= user.name %></h5>
                        <p><strong>Grades:</strong><%= user.grade %></p>
                    </div>
                    <div class="col-md-8">
                        <h4><%= user.name %></h4>
                        <ul class="list-group list-group-flush">
                            <li class="list-group-item"><strong>Course:</strong> <%= user.course %></li>
                            <li class="list-group-item"><strong>Roll No:</strong> <%= user.username %></li>
                            <li class="list-group-item"><strong>Certificate:</strong> <%= user.certificate %></li>
                            <li class="list-group-item"><strong>Joining Date:</strong><%= user.createdAt %></li>
                        </ul>
                     <form action="/api/v1/auth/Logout" method="POST">   <button type="submit" class="btn btn-danger mt-3" >Logout</button></form>
                     <form action="/api/v1/update/api/<%= user._id %>" method="post">
                     <button type="submit" class="btn btn-info mt-3" >Certificate</button>
                    </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap JS, Popper.js, and jQuery -->
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.min.js"></script>

<script>
    function logout() {
        // Logic for logging out (e.g., redirect to login page)
        alert("You have been logged out!");
        window.location.href = "login.html"; // Replace with actual login page URL
    }
</script>

</body>
</html>
