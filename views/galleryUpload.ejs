<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upload Image</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Arial', sans-serif;
    }
    .upload-container {
      max-width: 500px;
      margin: 50px auto;
      padding: 30px;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
    .upload-container h2 {
      text-align: center;
      color: #343a40;
      margin-bottom: 20px;
    }
    .form-control {
      margin-bottom: 15px;
    }
    .btn-upload {
      background-color: #007bff;
      color: #fff;
    }
    .btn-upload:hover {
      background-color: #0056b3;
    }
    .preview {
      margin-top: 20px;
      text-align: center;
    }
    .preview img {
      max-width: 100%;
      height: auto;
      border-radius: 10px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body>
  <div class="upload-container">
    <h2>Upload an Image</h2>
    <form id="uploadForm" action="/api/v1/upload/galleryUpload" method="POST" enctype="multipart/form-data">
      <div class="mb-3">
        <label for="title" class="form-label">Title</label>
        <input type="text" name="title" class="form-control" placeholder="Enter image title" required>
      </div>
      <div class="mb-3">
        <label for="image" class="form-label">Select Image</label>
        <input type="file" id="image" name="images" class="form-control" accept="image/*" required>
      </div>
      <input type="submit" value="Upload" class="btn btn-upload w-100">
      <!-- <button type="submit" class="btn btn-upload w-100">Upload</button> -->
    </form>
    <div class="preview" id="preview">
      <!-- Image preview will appear here -->
    </div>
  </div>

 
</body>
</html>
